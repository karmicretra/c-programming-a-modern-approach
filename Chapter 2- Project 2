#include <stdio.h>
#include <math.h>
//Chapter 2. Project 2 

void main()
{ 
    float volume = 4.0f/3.0f * 22.0/7.0 * (pow(10, 3));
    //pow(a, b) from the math header file, a is the base number, b is the power
    printf("%.3f", volume);
    //.3f print 3 digits after the decimsl point
}
